{% extends base.html %}

{% block content %}
<form>
    <a href="{{ content }}"> Click here to auth with twitter </a><br/>
    <label for="text">Enter pin</label><input type="text" name="pin" /><br/>
    <input type="Submit"/>
</form>
<a href="?pin=None">Don't log-in</a>

{% end %}
